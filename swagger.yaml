openapi: 3.0.0
info:
  title: Monthly report system
  description: 月報管理システムのAPI
  version: 0.0.0
servers:
  - url: https://api.example.com/v1
    description: 本番サーバーはここに記述
  - url: http://localhost:8080
    description: ローカルサーバー
paths:
  /users:
    get:
      summary: Returns a list of users.
      description: Optional extended description in CommonMark or HTML.
      responses:
        '200':    # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string